# Copilot Instructions

This is a WordPress CMS template for StackBlaze with custom plugin development.

## When checking for updates
- Check if the WordPress Docker base image tag is current (php8.3-apache)
- Verify the starter plugin uses current WordPress APIs
- Check for deprecated WordPress hooks or functions
- Ensure docker-compose.yml uses current MySQL image
- Keep the template minimal — starter scaffold, not a full theme